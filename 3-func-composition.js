// Lets say we want to get the input, trim it and wrap it on a div

let input = "    Javascript    ";
let output = `<div> ${input.trim()} </div>`;

// This is a NONE functional type of code, let work on this
